# Fullstack Trello Clone
A fullstack application that replicates the core functionality of Trello, allowing users to create boards, lists, and cards, and manage tasks efficiently.

# Start
```bash
# Client
cd client
npm install
npm run dev

# Server
cd server
npm install
npm run dev

# Start db
docker-compose up -d
```